Public Maryland OneStop exact-EIN entries responses captured September 14, 2026,
from the existing master checker during read-only baseline validation. Each JSON
fixture preserves the original response text, including escaped HTML in DBA
fields. Source: https://onestop.md.gov/list_views/62f3e1797f7e3200016a3dab

The primary name field is `f_aedd5545-808f-4725-9b1d-5fa61e994a75`.
The `Charity EIN` and `Charity ID` labels are scoped to that same entry.
These fixtures test display metadata; they do not define or change status rules.
