# Kentucky and New Hampshire PDF extraction - staging .6

Release: 2026.09.10.6-staging. Kentucky's loose-text reconstruction mixed neighboring addresses into Opportunity@Work, FoodCorps and Zearn. New Hampshire's line parser lost names when addresses wrapped. Both master loaders now read the drawn table cells and reconcile registration IDs on every page. KY retains name/DBA text and the last-filed year; NH retains the name, status flag and report due date. Existing matching, status, date calculations, comments and batch code are unchanged.

Fresh sources: Kentucky updated September 7 (downloaded September 10; 8,275 rows), New Hampshire updated September 10 (14,030 named rows, including nine source rows with blank IDs). The NH parsed asset is bound to its PDF hash and is generated by the master parser during refresh, matching the existing KY saved-data approach. This avoids a full PDF parse during a normal cold lookup. No runtime sidecar was added.

Local release gate: all 73 controls passed (the 30 approved KY cases, 30 NH cases, ten mature-state controls and three original reported cases). All 25 regression suites passed, including 14 new PDF extraction checks and six existing wrapped-row checks. Source fields matched the full-document table probes. KS, LA and OR data are unchanged. Production is not authorized and has not been touched.

Validation commands used the existing Python runtime with `-X utf8`:

- `refresh_downloadable_state_data.py --states KY` and `--states NH`
- `-m py_compile registry_snapshot_server.py refresh_downloadable_state_data.py`
- `testing/run_ky_nh_pdf_guardrails.py`
- `testing/run_downloadable_data_guardrails.py`
- Existing release suites and `testing/run_remaining12_guardrails.py`
- `git diff --check`

Detailed local evidence is in the workspace's outputs/ky-nh-release35-20260910 directory: local-summary.json, gate-summary.json and scope-audit.json. The source is the read-only Fresh Test / Orgs spreadsheet; no expected result was edited.

Deployment order: update both staging backends, update the live staging website, verify version/API targets and all dataset checksums, pass staging smoke, then run all 35 selected organizations across 29 states (1,015 checks; New York excluded by user instruction). Preserve accepted differences and stop scheduling new checks on a mismatch. Production promotion requires separate explicit approval after that review.
