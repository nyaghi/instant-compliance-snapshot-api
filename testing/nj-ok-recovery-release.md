# New Jersey and Oklahoma retrieval recovery

Scope: staging only. New Jersey now reads an already-open, populated detail frame, waits briefly for delayed fiscal-period evidence, and applies recovered dates to the explicitly incomplete Current fallback. Ordinary Current, exempt, closed, and adverse results retain their existing interpretation. Oklahoma submits specific, deduplicated name phrases before broad token probes, reserving broad fallback queries within the existing limit. Candidate acceptance, certificate interpretation, state budgets, shared matching, batch routing, infrastructure, and environment variables are unchanged. No organization exceptions or new statuses were added.

Validation commands: `python testing/run_nj_ok_recovery_guardrails.py`, the existing state/matching/report guardrail suites, `python testing/run_ak_batch_guardrails.py`, `python testing/run_weekly_data_smoke.py --cases <live-cases.json> --output <local-live.jsonl>`, `python -m py_compile registry_snapshot_server.py testing/run_nj_ok_recovery_guardrails.py`, and `git diff --check`.

The new seven-test suite covers delayed and already-open detail frames, unrelated record rejection, late-date application with terminal-status controls, general query ordering/deduplication, and broad-query fallback. Existing suite results and raw live evidence are retained under `outputs/nj-ok-surgical-20260909` in the parent workspace. The approved spreadsheet export is unchanged. The selected live group includes known current, upcoming, and no-record organizations in both states, plus a mature Colorado EIN-first control.

One inherited NMDP compatibility assertion fails identically on the unchanged baseline: its mocked browser-fetch response no longer represents the primary certificate request after the previously approved bounded-request fallback. The 29 dedicated certificate-recovery tests pass. This release does not change that certificate behavior or rewrite its old expectation. An attempted generic batch test filename did not exist; the actual `run_ak_batch_guardrails.py` suite was then run and passed.

Staging release target: backend 2026.09.09.4-staging, frontend 2026.09.09.4. Conditional deployment requires successful local controls. Post-deployment verification must confirm both staging backends, the visible frontend version/API target, preserved staging functions/routes, and a concurrent browser regression group. Final deployment IDs, comparisons, remaining limitations, and recommendation are recorded with the workspace evidence. No production changes or promotion are authorized.
