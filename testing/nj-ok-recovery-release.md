# New Jersey and Oklahoma retrieval recovery

Scope: staging only. New Jersey now reads an already-open, populated detail frame, waits briefly for delayed fiscal-period evidence, and applies recovered dates to the explicitly incomplete Current fallback. If that fallback still lacks dates, one bounded reload of the already-selected detail iframe can recover a stalled document. Its URL must be the NJ detail route with record identifiers, and its recovered identity and fiscal evidence must match before use. This adds at most a 15-second navigation wait plus an 8-second evidence poll and small locator overhead to this incomplete NJ path only. Ordinary Current, exempt, closed, and adverse results retain their existing interpretation. Oklahoma submits specific, deduplicated name phrases before broad token probes, reserving broad fallback queries within the existing limit. Candidate acceptance, certificate interpretation, shared matching, batch routing, infrastructure, and environment variables are unchanged. No organization exceptions or new statuses were added.

Validation commands: `python testing/run_nj_ok_recovery_guardrails.py`, the existing state/matching/report guardrail suites, `python testing/run_ak_batch_guardrails.py`, `python testing/run_weekly_data_smoke.py --cases <live-cases.json> --output <local-live.jsonl>`, `python -m py_compile registry_snapshot_server.py testing/run_nj_ok_recovery_guardrails.py`, and `git diff --check`.

The new ten-test suite covers delayed and already-open detail frames, unrelated record rejection, late-date application with terminal-status controls, bounded reload and URL validation, general query ordering/deduplication, and broad-query fallback. Existing suite results and raw live evidence are retained under `outputs/nj-ok-surgical-20260909` in the parent workspace. The approved spreadsheet export is unchanged. The selected live group includes known current, upcoming, and no-record organizations in both states, plus a mature Colorado EIN-first control.

One inherited NMDP compatibility assertion fails identically on the unchanged baseline: its mocked browser-fetch response no longer represents the primary certificate request after the previously approved bounded-request fallback. The 29 dedicated certificate-recovery tests pass. This release does not change that certificate behavior or rewrite its old expectation. An attempted generic batch test filename did not exist; the actual `run_ak_batch_guardrails.py` suite was then run and passed.

The first staging iteration, 2026.09.09.4, passed all 15 local live controls. Its 15 concurrent browser sessions completed all 45 NJ/OK/CO checks with 43 matches. Arbor Day NJ and RMHC NJ retained the incomplete Current fallback. All OK and CO results matched. A subsequent live diagnostic reproduced a partial NJ detail document: the correct iframe source URL was present, but the frame contained only a 506-character page header with no fiscal fields. This evidence motivated the bounded selected-frame reload. The original failing browser evidence is preserved under `outputs/browser-capacity-20260909/nj-ok-co15-v4*`.

Final staging release target: backend 2026.09.09.5-staging, frontend 2026.09.09.5. Conditional deployment requires successful controlled live recovery. Post-deployment verification must confirm both staging backends, the visible frontend version/API target, preserved staging functions/routes, and a repeat concurrent browser regression group. Final deployment IDs, comparisons, remaining limitations, and recommendation are recorded with the workspace evidence. No production changes or promotion are authorized.
