# Evidence-based comments — 2026.09.06.10 staging

User requested a complete review of status explanations across all supported states. Replaced two overlapping comment generators (including generic early returns that hid available dates) with one master-owned evidence-to-reason-to-status formatter. Existing classification, lookup, matching and batch functions are unchanged. No runtime sidecar or organization-specific data added.

Comments now name an observed status precisely, or identify the filing year/period and the date used, explain whether that date has passed, is today, is within six calendar months or is farther away, and connect that evidence to the displayed status. Raw suspension, revocation, pending, exemption, closure and failed-renewal labels remain distinct. Calendar timing is derived from the date, never guessed from the displayed status. Date extraction is restricted to labeled evidence fields; original registration dates and unrelated page dates are not used as deadlines.

Oklahoma certificate, calculated-anniversary and cached-certificate explanations remain distinct. Mississippi's fallback date is disclosed as calculated. All five downloadable-data freshness notes and registry-match metadata remain. Missing/ambiguous evidence and failed searches receive explanations of what could not be established, without claiming they prove delinquency or non-registration. When existing classification relies on unavailable annual filings, the comment explicitly identifies that inference and its limitation. Virginia extension/base-date conflicts are exposed, not hidden by wording.

Validation before deployment: 145 guardrails passed (26 comment, 30 Oklahoma, 16 reconciliation, 14 New York, 30 matching, 10 downloadable-data, 19 PDF). A saved audit covers 452 first-result summaries; older summaries omit some original page evidence and are not a substitute for a new live check. Thirty live local Make-A-Wish state checks and eight targeted exception checks captured full page evidence. Replays confirm comments do not mutate results and require no network calls; state/profile context is reused only when already loaded by the status pass. Code-scope checks compare function syntax trees to verify existing non-comment functions are unchanged. Compilation and diff checks are required before push.

Deployment/live regression evidence and the final review are under outputs/comment-rationale-20260906. Approved spreadsheet expectations are preserved. Production is not authorized. Do not promote until outstanding broader discrepancies and evidence limitations have been reviewed.

Final .11 follow-up: browser failure explanations use plain language. Retry conversions replace obsolete negative explanations, and batch follow-ups explain the evidence used. Retry selection and classification are unchanged. 29 comment tests plus 119 existing guardrails and six browser scenarios pass. All 150 staging classifications matched the preceding run; 136 matched the unchanged manual baseline. One first request used .9 during rolling deployment and is retained with its version, with a separate final-version recheck.
