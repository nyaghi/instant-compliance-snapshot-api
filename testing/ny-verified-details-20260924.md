# New York verified record details — September 24, 2026

America's Charities (54-1517707), registry ID 04-31-23, was found by the browser search, but the server's subsequent RegistryDetail request returned HTTP 401 with `Invalid recaptcha token.` The ordinary verified state page displayed the organization and its annual filing history. The released validation path reproduced the failure without the Sales deadline or cancellation signal. This was a split-transport defect: search ran in the verified browser, while positive details were fetched separately by the backend. The exact date of the state's verification requirement change has not been established.

Connector 0.4.1 opens the exact master-selected record link in the same browser session and returns only allowlisted public identity fields and filing dates. Tokens, cookies, authorization headers, and document download URLs are not transferred. The backend binds details to the issued record ID and applies the existing EIN, name, address, duplicate-selection, and filing rules. Same-EIN duplicate details use the browser Back button to preserve the search context. Completion also closes owned detail tabs; unrelated user tabs remain open.

Completed empty annual history remains distinct from unavailable history. Explicit exempt records retain their existing treatment when the state omits filing history. Old connectors receive an update explanation when the separate detail request returns 401. Searches and detail requests are separately bounded to five each within the existing signed-check lifetime. Standard scheduling, discovery, and state interpretation are unchanged. The previously deployed Sales 60-second deadline remains in place.

Controls cover current, stale and empty-history delinquency, exemption, completed no-record searches, wrong EIN/record, duplicate identities, incomplete history, blocked detail requests, token isolation, cleanup, interrupted sessions, 15 queued organizations, and the Sales deadline. The real extension browser harness observes fixture search and detail traffic through the same page/worker/master pipeline.

Release target: staging UI/backend 2026.09.24.2, connector 0.4.1. Production deployment is not authorized. Live deployment and post-validation evidence is recorded in the workspace output folder `outputs/ny-401-20260924`.
